import 'package:flutter/material.dart';

class TravauxDetails extends StatelessWidget {
  const TravauxDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('Travaux'),
        ),
        body: Container());
  }
}
